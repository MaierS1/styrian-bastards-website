{
  "scripts": {
    "check:encoding": "node scripts/check-encoding.js",
    "check:hero": "node scripts/check-hero.js",
    "build": "npm run check:encoding && npm run check:hero && node scripts/build.js",
    "dev": "node scripts/dev.js"
  }
}
